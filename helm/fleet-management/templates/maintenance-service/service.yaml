apiVersion: v1
kind: Service
metadata:
  name: maintenance-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "fleet-management.labels" . | nindent 4 }}
    app: maintenance-service
spec:
  selector:
    app: maintenance-service
  ports:
    - protocol: TCP
      port: {{ .Values.maintenanceService.port }}
      targetPort: {{ .Values.maintenanceService.port }}
  type: ClusterIP
